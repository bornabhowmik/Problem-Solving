#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    
    while (t--) {
        int x;
        scanf("%d", &x);
        
        int a = x*10;
        
        printf("%d/n", a);
        
        return 0;
    }
}
